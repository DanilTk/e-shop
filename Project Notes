Client:
    - add id creation method

ProductRepository:
    - Revise return types of classes
    - Class is needed for DataBase operations - please confirm?

Other:
    - change of parameters order - does it affect the end code?
    - Boolean vs. boolean
    - Enums cannot be used as parameters in methods if aren't declared separately as classes outside of the class

The below methods probably should be moved to another class >> BasketServiceImpl?

        filterProducts
        findTopNumberOfProductsByCategory
        groupByProductCategory




Interesting way to write ifs - why there are no {}?:

   if (validateProductName(product)) if (validateProductQuantity(product)) if (validateProductPrice(product))
            if (validateProductMeasureName(product)) if (validateProductCategory(product)) return true;
        return false;